name: kargo_sistemi
description: "Kargo Sistemi — Akıllı Kargo Dolabı UI/UX demo (backend yok)."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  google_fonts: ^6.2.1
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand_logo.png
